VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173
# Default when backend runs inside the devcontainer alongside the frontend:
VITE_API_URL=http://localhost:3000
# Uncomment the next line instead if the backend runs on your host machine:
# VITE_API_URL=http://host.docker.internal:3000
